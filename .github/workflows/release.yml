name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      republish_version:
        description: 'Re-publish an existing tag to npm (e.g. 0.3.0). Leave empty for auto catch-up.'
        required: false
        type: string

permissions:
  contents: read

jobs:
  release:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      released: ${{ steps.direct.outputs.released }}
      version: ${{ steps.direct.outputs.version }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          fetch-depth: 0

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '24'

      - name: Prepare release (tag vX.Y.Z, no Release PR)
        id: direct
        run: node scripts/direct-release.mjs

      - name: Commit, tag, and push
        if: steps.direct.outputs.released == 'true'
        env:
          VERSION: ${{ steps.direct.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "Release v${VERSION} already committed on main; tagging HEAD only."
          else
            git commit -m "chore(release): publish v${VERSION}"
            git push origin main
          fi
          if git rev-parse "v${VERSION}" >/dev/null 2>&1; then
            echo "Tag v${VERSION} already exists locally."
          else
            git tag "v${VERSION}"
          fi
          git push origin "refs/tags/v${VERSION}" || git push origin "v${VERSION}"

      - name: Create GitHub Release
        if: steps.direct.outputs.released == 'true'
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          tag_name: v${{ steps.direct.outputs.version }}
          body_path: release-body.md
          generate_release_notes: false

  resolve-publish:
    runs-on: ubuntu-latest
    needs: release
    if: always() && (github.event_name == 'workflow_dispatch' || needs.release.result != 'skipped')
    permissions:
      contents: read
    outputs:
      publish: ${{ steps.pick.outputs.publish }}
      version: ${{ steps.pick.outputs.version }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          fetch-depth: 0

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '24'

      - name: Pick npm version to publish
        id: pick
        env:
          RELEASED: ${{ needs.release.outputs.released }}
          RELEASE_VERSION: ${{ needs.release.outputs.version }}
          REPUBLISH_VERSION: ${{ github.event.inputs.republish_version }}
        run: |
          if [ -n "$REPUBLISH_VERSION" ]; then
            if ! printf '%s' "$REPUBLISH_VERSION" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+$'; then
              echo "::error::republish_version must be semver (e.g. 1.2.3), got: $REPUBLISH_VERSION"
              exit 1
            fi
            echo "publish=true" >> "$GITHUB_OUTPUT"
            echo "version=$REPUBLISH_VERSION" >> "$GITHUB_OUTPUT"
            echo "Manual republish: v$REPUBLISH_VERSION"
          elif [ "$RELEASED" = "true" ] && [ -n "$RELEASE_VERSION" ]; then
            echo "publish=true" >> "$GITHUB_OUTPUT"
            echo "version=$RELEASE_VERSION" >> "$GITHUB_OUTPUT"
            echo "Fresh release: v$RELEASE_VERSION"
          else
            node scripts/npm-publish-catchup.mjs
          fi

  publish-npm:
    needs: resolve-publish
    if: needs.resolve-publish.outputs.publish == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: v${{ needs.resolve-publish.outputs.version }}

      # Trusted Publisher (OIDC): workflow filename must match npm settings: release.yml
      # Do not npm install -g npm@latest here — breaks sigstore for --provenance (npm/cli#8018).
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '24'
          cache: npm
          registry-url: 'https://registry.npmjs.org'

      - run: npm ci
      - run: npm run sync-versions
      - run: npm publish --provenance --access public

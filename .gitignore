node_modules/
dist/
review-harness/
*.tsbuildinfo
.DS_Store
.env
.env.*

# Local only — never commit, never CHANGELOG, never link from tracked docs
research/
.cursor/
release-body.md

# Local CI/coverage artifacts
tmp-base-coverage.json
tmp-parse-transcript.mjs
tmp-transcript-parse.json

# Template is tracked; real values never are
!.env.example
